---
description: 讀取 design.md 並產生 task.md。若 design.md 有「待討論問題」會先進入討論模式
argument-hint: <task-name>
allowed-tools: Read, Write, Edit, Bash(test:*), Bash(grep:*), Bash(stat:*), Bash(wc:*), Bash(cat:*), Bash(awk:*), Bash(sed:*), Bash(date:*)
---

# /spec:task — 產生 task.md(含討論模式)

使用者要求為 `$ARGUMENTS` 產生 task.md。

## 你的任務

### Step 1:確認檔案存在

!`test -f specflow/changes/$ARGUMENTS/design.md`

若 exit code != 0 → **立即停止**並提醒使用者「請先執行 /spec:design $ARGUMENTS」。

### Step 2:【關鍵】用 cat 強制重新讀取 design.md

⚠️ **重要**:`design.md` 是使用者剛剛在編輯器中審查、勾選、可能寫了討論問題的檔案。
**不可使用 Read 工具**——Read 工具有快取機制。**必須使用 cat**:

!`cat specflow/changes/$ARGUMENTS/design.md`

⚠️ 上述 cat 命令的輸出**就是 design.md 的真實內容**,以此為準。

### Step 3:檢查兩個閘門條件

`/spec:task` 的閘門條件是**兩個 AND**:

1. **決策清單全部勾選**(沒有 `- [ ]` 項目)
2. **「待討論問題」區塊清空**(沒有實質內容,只剩 template 說明文字)

任一條件不滿足,執行對應的處理流程。

#### 條件 1 檢查:決策清單

統計未勾選項目數:

!`grep -cE "^\s*- \[ \]" specflow/changes/$ARGUMENTS/design.md`

記下這個數字,稱為 `unchecked_count`。

#### 條件 2 檢查:待討論問題區塊

擷取「## 待討論問題」區塊的內容:

!`awk '/^## 待討論問題/,0' specflow/changes/$ARGUMENTS/design.md`

判斷此區塊是否有「實質討論問題」:

- ✅ **有實質問題**:存在不在 `>` 引用區塊內、不在 `<!-- -->` 註解內、且看起來像是使用者真的寫的問題的 bullet 點(例如「- 決策 4 - 是否要加 is_active 欄位?」)
- ❌ **無實質問題**:區塊只剩 template 預設的說明文字(`>` 開頭的引用、`<!-- -->` 註解、空行)

判斷原則:**仰賴你的閱讀理解**,不要被 template 說明文字干擾。
template 的引用區塊(`> ...`)跟註解(`<!-- ... -->`)都不算「使用者寫的問題」。

### Step 4:依條件分支執行

根據 Step 3 的兩個檢查結果分支:

| 條件 1(決策清單) | 條件 2(待討論問題) | 執行的流程 |
|---|---|---|
| 全勾選 | 無問題 | **A. 產 task 模式** |
| 全勾選 | 有問題 | **B. 討論模式** |
| 未全勾選 | 無問題 | **C. 提示使用者勾選決策** |
| 未全勾選 | 有問題 | **B. 討論模式**(優先處理討論) |

### 流程 A:產 task 模式

(條件 1 + 條件 2 都滿足)

#### A-1:檢查 task.md 是否已存在

!`test -f specflow/changes/$ARGUMENTS/task.md`

若 exit code = 0(已存在),**停下來問使用者**:「task.md 已存在,要覆蓋嗎?(y/n)」

#### A-2:讀取相關檔案

- `specflow/project.md` → 用 Read 工具(穩定)
- `specflow/changes/$ARGUMENTS/issue.md` → 用 cat:`!cat specflow/changes/$ARGUMENTS/issue.md`
- `design.md` 已在 Step 2 用 cat 讀過,不必重讀

#### A-3:產生 task.md

依照 `.claude/skills/specflow/templates/task.md` 格式產出。

**強制要求**:

- 每項任務是一個 checkbox,顆粒度為**5 分鐘內可完成**
- 每項任務必須包含「**檔案路徑** + **改動內容**」兩個子項
- 涉及新 Service 時,任務必須包含三步驟(依 project.md §3):
  1. 建立 Core interface(`app/Core/Services/{Module}/Contracts/`)
  2. 建立 implementation(`app/Services/{Module}/`)
  3. 注入到使用方
- 「驗證」區塊必須列出測試命令、coding style 檢查、其他手動驗證
- 「執行後備註」區塊**保持空白**(由 /spec:run 階段填寫)
- 整份 task.md 必須使用**繁體中文**撰寫

#### A-4:寫入並回報

使用 Write 工具寫入 `specflow/changes/$ARGUMENTS/task.md`,告知使用者:

> ✅ 已產生 `specflow/changes/$ARGUMENTS/task.md`
>
> 請審查執行清單。確認後執行:`/spec:run $ARGUMENTS`

### 流程 B:討論模式

(無論條件 1 是否滿足,只要條件 2 有問題就進入此流程)

#### B-1:讀取背景資料

- `specflow/project.md` → 用 Read 工具
- `specflow/changes/$ARGUMENTS/issue.md` → 用 cat

#### B-2:取得當前日期(用於記錄到「已討論問題」)

!`date +%Y-%m-%d`

記下這個日期,稱為 `today`,後續寫入「已討論問題」時使用。

#### B-3:逐題處理

對「待討論問題」區塊中的**每一個使用者寫的問題**:

1. **理解問題**:讀懂使用者在問什麼,屬於哪條決策
2. **回答問題**:基於 project.md、issue.md、現有 design.md 的脈絡給出技術判斷
3. **判斷是否需要修改決策**:
   - 若使用者的疑問引出了應該調整的點 → **需要修改受影響的決策**
   - 若只是請求解釋(例:「為什麼選這個索引順序?」)且原決策仍合理 → **不修改決策**,僅記錄解釋

#### B-4:修改 design.md(三個動作)

使用 **Edit 工具**對 design.md 做以下修改:

##### 動作 1:修改受影響的決策

- 若決策需要修改:直接覆蓋決策內容(**不保留歷史版本**;歷史交給「已討論問題」+ git)
- 若決策不需修改:跳過此動作
- 若決策被修改了:把該決策的 `- [x]` 改回 `- [ ]`(需要重新審查)
- 若決策原本就是 `- [ ]`(使用者還沒勾):維持 `- [ ]`

##### 動作 2:把問題摘要追加到「已討論問題」區塊

⚠️ 這是**追加**,不是覆蓋。如果區塊裡已經有舊的討論記錄,**保留它們**,把新記錄加在最後。

對每個處理過的問題,寫入下列格式:

```markdown
### N. 決策 X - <問題主題的精簡標題>
- **問題**:<使用者原問題的簡短復述>
- **結論**:<你的技術判斷,1~2 句話>
- **影響**:<選擇下列其一>
  - 決策 X 已更新:<簡述更新內容>
  - 未修改決策(僅補充說明)
- **討論時間**:`today`(Step B-2 取得的日期)
```

⚠️ `N` 的編號是**連續的**——若區塊裡已經有 `### 1.`、`### 2.`,新的就從 `### 3.` 開始。
若區塊原本是空的(只有 template 註解),從 `### 1.` 開始。

⚠️ Edit 工具的 old_str 應該定位到 template 註解 `<!-- 討論記錄會自動追加在這裡。初始狀態為空。 -->`
**之後**(下一行開始)插入新記錄;**保留**這個註解。
若區塊已經有舊記錄,則 old_str 定位到最後一個記錄的尾巴,在後面追加。

##### 動作 3:清空「待討論問題」區塊

- 刪除使用者寫的所有問題 bullet
- **保留** template 的說明文字(`>` 引用區塊、`<!-- -->` 註解)
- 這樣下次 cat 看到的「待討論問題」區塊會回到空白狀態

⚠️ **不要修改「決策清單」+「已討論問題」+「待討論問題」以外的區塊**
(影響範圍、實作細節、降級策略),除非那些區塊的內容真的因為討論結論而需要更新。

#### B-5:回報處理結果

告知使用者:

> ## 討論處理完成
>
> ### 處理的問題
>
> 1. **問題**: [使用者原問題]
>    **回答**: [你的技術判斷]
>    **動作**: [修改了決策 X / 未修改決策(僅解釋)]
>
> 2. ...
>
> ### 變更摘要
>
> - 修改的決策: [列出決策編號與標題]
> - 重置為待審查的 checkbox: [N 個]
> - 已寫入「已討論問題」: [N 筆記錄]
> - 「待討論問題」區塊: 已清空
>
> ### 下一步
>
> 請審查 design.md 中變更過的決策,確認後勾選 checkbox。
> 若有新的疑問,可繼續寫入「待討論問題」區塊。
> 全部勾選且無新問題後,重新執行 `/spec:task $ARGUMENTS`。

#### B-6:停止

**不要繼續產 task.md**。即使你修改完決策後檢查發現「決策清單剛好全勾選 + 待討論清空」,
也不要產 task.md,因為:

1. 你剛才把修改過的決策的 `[x]` 改回 `[ ]`,所以條件 1 必然不滿足
2. 即使有 edge case 讓條件 1 仍滿足(例如使用者只問了「為什麼這樣設計」沒改決策),
   仍應停下來讓使用者重新檢視

### 流程 C:提示使用者勾選決策

(條件 1 未滿足,條件 2 已清空)

告知使用者:

> ⚠️ design.md 中仍有 N 個未勾選的決策項。
>
> 請在編輯器中將同意的決策從 `- [ ]` 改為 `- [x]`,
> 或修改該項內容後勾選。
>
> 全部勾選後重新執行 `/spec:task $ARGUMENTS`。

## 硬規則

- ❌ **絕對不要在「決策清單未全勾選」+「待討論問題清空」時產出 task.md**
- ❌ **絕對不要在討論模式中自動產 task.md**(無論決策清單看起來多完整)
- ❌ **絕對不要自動執行 task**(這是 /spec:run 的職責)
- ❌ **絕對不要在討論模式中修改「待討論問題以外」的內容,除了:
  (a) 受問題影響的決策內容
  (b) 受影響決策的 checkbox 狀態
  (c) 把問題摘要追加到「已討論問題」區塊
  (d) 清空「待討論問題」區塊**
- ❌ **「已討論問題」是追加,不是覆蓋**(若已有舊記錄,新記錄加在最後)
- ❌ task 顆粒度**禁止超過 5 分鐘**(超過要拆分)
- ❌ **絕對不要用 Read 工具讀 design.md / issue.md**(必須用 cat 繞過快取)
- ❌ **絕對不要說「沒有變更」這種仰賴記憶的判斷**
